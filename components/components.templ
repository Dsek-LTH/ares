package components

templ Index(name string) {
	<h1>Hello, { name }</h1>
}

templ Admin() {
	<h1>Admin</h1>
}

templ Signup(name, stilId string, createdNewAccount bool) {
	<h1>Sign up</h1>
	<div>
		<p> Name: { name } </p>
		<p> Stil-id: { stilId } </p>
		if createdNewAccount {
			<p>Successfully completed user creation progress</p>
		}
	</div>
}

templ Leaderboard() {
	<h1>Leaderboard</h1>
}
